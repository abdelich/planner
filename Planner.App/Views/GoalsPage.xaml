<UserControl x:Class="Planner.App.Views.GoalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:vm="clr-namespace:Planner.App.ViewModels"
      xmlns:models="clr-namespace:Planner.App.Models"
      Background="{StaticResource BackgroundDarkBrush}">
    <UserControl.Resources>
        <vm:GoalsViewModel x:Key="Vm"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="GoalTypes">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:GoalType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="GoalCategories">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:GoalCategory"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="RecurrenceKinds">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:RecurrenceKind"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid x:Name="RootGrid" DataContext="{StaticResource Vm}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Цели" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <Button Content="+ Новая цель" Command="{Binding OpenAddPanelCommand}" Style="{StaticResource PrimaryButton}" Margin="24,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="На сегодня" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                <ItemsControl ItemsSource="{Binding TodayDue}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BackgroundHoverBrush}" CornerRadius="8" Padding="12,8" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsCompletedToday, Mode=OneWay}" Command="{Binding DataContext.ToggleTodayCompleteCommand, ElementName=RootGrid}" CommandParameter="{Binding}" Style="{StaticResource CheckBoxStyle}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Goal.Title}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding FrequencyText, Mode=OneWay}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding ProgressText, Mode=OneWay}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="border" Padding="16,10" Margin="0,0,4,0" Background="{StaticResource BackgroundCardBrush}" CornerRadius="8,8,0,0" BorderBrush="#e0e0e0" BorderThickness="1,1,1,0">
                                    <ContentPresenter ContentSource="Header"/>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                                                </Trigger>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                </Style>
            </TabControl.Resources>
            <TabItem Header="На день / неделю / месяц">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="Transparent" Padding="8,4" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="←" Command="{Binding PrevPeriodCommand}" Style="{StaticResource NavButton}" Padding="12,6" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding DisplayPeriodText}" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="→" Command="{Binding NextPeriodCommand}" Style="{StaticResource NavButton}" Padding="12,6"/>
                        </StackPanel>
                    </Border>
                    <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedPeriodSubTabIndex}" Background="Transparent" BorderThickness="0" Margin="0,0,0,0">
                    <TabItem Header="День">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding DayGoals}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource CardBorder}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPastPeriod}" Value="True">
                                                            <Setter Property="Opacity" Value="0.75"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DataContext.DisplayPeriodText, ElementName=RootGrid}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Goal.Title}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        <TextBlock Text="{Binding Goal.Description, TargetNullValue=''}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                        <TextBlock Text="{Binding StatusText, Mode=OneWay}" Foreground="#c62828" FontSize="13" Margin="0,4,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding StatusText}" Value="Не выполнено">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <TextBlock Margin="0,6,0,0">
                                                            <Run Text="Прогресс: " Foreground="{StaticResource TextSecondaryBrush}"/>
                                                            <Run Text="{Binding ProgressText, Mode=OneWay}" Foreground="{StaticResource AccentLightBrush}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Button Content="✓ Выполнено" Command="{Binding MarkCompleteCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" IsEnabled="{Binding CanMarkComplete}"/>
                                                        <Button Content="Удалить" Command="{Binding DataContext.DeleteGoalCommand, ElementName=RootGrid}" CommandParameter="{Binding}" Style="{StaticResource NavButton}" Foreground="#c62828"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="100" Style="{StaticResource ProgressBarStyle}" Margin="0,12,0,0" Height="8"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Неделя">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding WeekGoals}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource CardBorder}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPastPeriod}" Value="True">
                                                            <Setter Property="Opacity" Value="0.75"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DataContext.DisplayPeriodText, ElementName=RootGrid}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Goal.Title}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        <TextBlock Text="{Binding Goal.Description, TargetNullValue=''}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                        <TextBlock Text="{Binding StatusText, Mode=OneWay}" Foreground="#c62828" FontSize="13" Margin="0,4,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding StatusText}" Value="Не выполнено">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <TextBlock Margin="0,6,0,0">
                                                            <Run Text="Прогресс: " Foreground="{StaticResource TextSecondaryBrush}"/>
                                                            <Run Text="{Binding ProgressText, Mode=OneWay}" Foreground="{StaticResource AccentLightBrush}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Button Content="✓ Выполнено" Command="{Binding MarkCompleteCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" IsEnabled="{Binding CanMarkComplete}"/>
                                                        <Button Content="Удалить" Command="{Binding DataContext.DeleteGoalCommand, ElementName=RootGrid}" CommandParameter="{Binding}" Style="{StaticResource NavButton}" Foreground="#c62828"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="100" Style="{StaticResource ProgressBarStyle}" Margin="0,12,0,0" Height="8"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Месяц">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding MonthGoals}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource CardBorder}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPastPeriod}" Value="True">
                                                            <Setter Property="Opacity" Value="0.75"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <TextBlock Text="{Binding DataContext.DisplayPeriodText, ElementName=RootGrid}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Goal.Title}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        <TextBlock Text="{Binding Goal.Description, TargetNullValue=''}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                        <TextBlock Text="{Binding StatusText, Mode=OneWay}" Foreground="#c62828" FontSize="13" Margin="0,4,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding StatusText}" Value="Не выполнено">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <TextBlock Margin="0,6,0,0">
                                                            <Run Text="Прогресс: " Foreground="{StaticResource TextSecondaryBrush}"/>
                                                            <Run Text="{Binding ProgressText, Mode=OneWay}" Foreground="{StaticResource AccentLightBrush}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Button Content="✓ Выполнено" Command="{Binding MarkCompleteCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" IsEnabled="{Binding CanMarkComplete}"/>
                                                        <Button Content="Удалить" Command="{Binding DataContext.DeleteGoalCommand, ElementName=RootGrid}" CommandParameter="{Binding}" Style="{StaticResource NavButton}" Foreground="#c62828"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="100" Style="{StaticResource ProgressBarStyle}" Margin="0,12,0,0" Height="8"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
                </Grid>
            </TabItem>
            <TabItem Header="Повторяемые">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                    <ItemsControl ItemsSource="{Binding RecurringGoals}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardBorder}">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Goal.Title}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding FrequencyText, Mode=OneWay}" Foreground="{StaticResource AccentLightBrush}" Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding Goal.Description, TargetNullValue=''}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                <TextBlock Margin="0,6,0,0">
                                                    <Run Text="Сегодня: " Foreground="{StaticResource TextSecondaryBrush}"/>
                                                    <Run Text="{Binding ProgressText, Mode=OneWay}" Foreground="{StaticResource AccentLightBrush}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="✓ Выполнено" Command="{Binding MarkCompleteCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                                                <Button Content="Удалить" Command="{Binding DataContext.DeleteGoalCommand, ElementName=RootGrid}" CommandParameter="{Binding}" Style="{StaticResource NavButton}" Foreground="#c62828"/>
                                            </StackPanel>
                                        </Grid>
                                        <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="100" Style="{StaticResource ProgressBarStyle}" Margin="0,12,0,0" Height="8"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Border Grid.RowSpan="3" Background="#CC000000" Visibility="{Binding IsAddPanelOpen, Converter={StaticResource BoolToVis}}" MouseDown="AddPanelOverlay_MouseDown">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Style="{StaticResource CardBorder}" Width="420" Margin="20" MouseDown="AddPanel_MouseDown">
                    <StackPanel>
                        <TextBlock Text="Новая цель" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                        <TextBlock Text="Название" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewGoalTitle, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" Margin="0,0,0,12" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" CaretBrush="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1"/>
                        <TextBlock Text="Описание (необязательно)" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewGoalDescription, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" Margin="0,0,0,12" Height="50" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" CaretBrush="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1"/>

                        <StackPanel Visibility="{Binding NewGoalCategory, Converter={StaticResource GoalCategoryToVisibilityConverter}}">
                            <TextBlock Text="Период" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding PeriodTypeLabel}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" Margin="0,0,0,12" Visibility="{Binding ShowPeriodTypeAsLabel, Converter={StaticResource BoolToVis}}"/>
                            <StackPanel Visibility="{Binding ShowPeriodTypeComboBox, Converter={StaticResource BoolToVis}}">
                                <ComboBox ItemsSource="{Binding Source={StaticResource GoalTypes}}" SelectedItem="{Binding NewGoalType}" Margin="0,0,0,12" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="#e0e0e0">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource GoalTypeToRu}}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                            <TextBlock Text="Цель в периоде (раз)" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding NewGoalTargetCount, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" Margin="0,0,0,16" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" CaretBrush="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>

                        <StackPanel Visibility="{Binding NewGoalCategory, Converter={StaticResource RecurringCategoryToVisibilityConverter}}">
                            <TextBlock Text="Как часто" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource RecurrenceKinds}}" SelectedItem="{Binding NewGoalRecurrenceKind}" Margin="0,0,0,12" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="#e0e0e0">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource RecurrenceKindToRu}}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <StackPanel Visibility="{Binding NewGoalRecurrenceKind, Converter={StaticResource EveryNDaysToVisibilityConverter}}">
                                <TextBlock Text="Каждые сколько дней" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding NewGoalIntervalDays, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" Margin="0,0,0,12" Background="{StaticResource BackgroundCardBrush}" Foreground="{StaticResource TextPrimaryBrush}" CaretBrush="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                            <StackPanel Visibility="{Binding NewGoalRecurrenceKind, Converter={StaticResource SpecificDaysToVisibilityConverter}}">
                                <TextBlock Text="Дни недели" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <WrapPanel>
                                    <CheckBox Content="Пн" IsChecked="{Binding NewGoalDayMon}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Вт" IsChecked="{Binding NewGoalDayTue}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Ср" IsChecked="{Binding NewGoalDayWed}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Чт" IsChecked="{Binding NewGoalDayThu}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Пт" IsChecked="{Binding NewGoalDayFri}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Сб" IsChecked="{Binding NewGoalDaySat}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                    <CheckBox Content="Вс" IsChecked="{Binding NewGoalDaySun}" Style="{StaticResource CheckBoxStyle}" Margin="0,0,12,6"/>
                                </WrapPanel>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <Button Content="Отмена" Command="{Binding CloseAddPanelCommand}" Style="{StaticResource NavButton}" Margin="0,0,8,0"/>
                            <Button Content="Сохранить" Command="{Binding SaveNewGoalCommand}" Style="{StaticResource PrimaryButton}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
